<%if @menu_pages && !@menu_pages.empty?%>
	<li style="border-left:none;"><%=link_to t("common.home"), "/"%></li>
	<%@menu_pages.each do |page|%>
		<li<%if @page && @page.is_a?(Page) && @page==page%> class="selected"<%end%>><%= link_to page.title.capitalize, page_path(page) %></li>
	<%end%>
<%end%>